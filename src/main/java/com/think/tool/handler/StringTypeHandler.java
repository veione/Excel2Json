package com.think.tool.handler;

import org.apache.poi.ss.usermodel.Cell;

public class StringTypeHandler extends AbstractTypeHandler {

    @Override
    public Object handle(Cell cell) {
        return super.handle(cell);
    }
}
